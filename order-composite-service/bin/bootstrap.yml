spring:
  application:
    name: order-composite-service
server:
  port: 0
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:9091/eureka}
  instance:
    preferIpAddress: true